# sketchpad/

Unpolished scratch scripts kept for reference (curve fitting experiments, a
per-region bandit prototype, an old end-to-end run in `test01.py`). They are
not maintained, not tested, and not guaranteed to run against the current
API. For working examples see `README.md` and `doc/source/guide_usage.rst`.
